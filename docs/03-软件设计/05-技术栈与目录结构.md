# 智慧酒店物联网控制系统 - 技术栈与目录结构说明

## 📚 技术栈与目录结构

本文档详细说明智慧酒店物联网控制系统的技术栈选择和项目目录结构。

---

## 🛠️ 技术栈详解

### 1.1 前端技术栈

#### 1.1.1 核心框架

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Vue.js** | 3.4+ | 前端框架 | 成熟的渐进式框架，生态完善，性能优秀，支持 Composition API |
| **TypeScript** | 5.4+ | 类型系统 | 类型安全，提升代码质量和可维护性，适合大型项目 |
| **Vite** | 5.2+ | 构建工具 | 现代化构建工具，开发体验优秀，冷启动快 |

#### 1.1.2 UI组件库

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Ant Design Vue** | 4.2+ | UI组件库 | 企业级UI组件库，组件丰富，文档完善，与Vue 3完美集成 |
| **Chart.js** | 4.4+ | 图表库 | 简单易用的图表库，支持多种图表类型 |
| **ECharts** | 5.4+ | 数据可视化 | 百度开源的强大图表库，支持复杂的数据可视化需求 |

#### 1.1.3 状态管理与路由

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Pinia** | 2.1+ | 状态管理 | Vue 3官方推荐的状态管理方案，简洁易用 |
| **Vue Router** | 4.3+ | 路由管理 | 官方路由管理，支持懒加载，性能优秀 |
| **Axios** | 1.6+ | HTTP客户端 | 基于Promise的HTTP客户端，支持拦截器 |

#### 1.1.4 实时通信

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Socket.io-client** | 4.7+ | WebSocket客户端 | 支持自动重连，兼容性好，功能强大 |

#### 1.1.5 工具库

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Day.js** | 1.11+ | 日期处理 | 轻量级日期处理库，API简洁 |
| **Lodash-es** | 4.17+ | 工具函数 | 提供丰富的工具函数，提升开发效率 |
| **VueUse** | 10.7+ | Vue组合式工具 | 提供常用的组合式函数，简化开发 |

---

### 1.2 后端技术栈

#### 1.2.1 核心框架

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Node.js** | 20.x LTS | 运行环境 | 事件驱动、非阻塞I/O，适合I/O密集型应用 |
| **Express.js** | 4.18+ | Web框架 | 简洁灵活的Web框架，生态成熟，社区活跃 |
| **TypeScript** | 5.4+ | 类型系统 | 类型安全，提升大型项目可维护性 |

#### 1.2.2 数据库

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **MySQL** | 8.0 | 关系型数据库 | 成熟稳定的关系型数据库，事务支持完善 |
| **Redis** | 7.0 | 缓存数据库 | 内存数据库，高性能缓存和会话管理 |
| **MQTT Broker** | 2.0 | 消息代理 | 轻量级消息代理，适合物联网场景 |

#### 1.2.3 实时通信

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Socket.io** | 4.7+ | WebSocket服务 | WebSocket封装，支持自动重连和降级 |
| **MQTT** | 3.1.1 | 物联网协议 | 轻量级发布/订阅协议，适合低带宽网络 |

#### 1.2.4 AI服务

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **智谱GLM-4** | Flash | 大语言模型 | 国产大模型，API友好，成本低，性能优秀 |
| **阿里云ASR** | - | 语音识别 | 语音识别准确率高，支持多种语言 |
| **讯飞TTS** | - | 语音合成 | 语音合成自然度高，支持多种音色 |

#### 1.2.5 开发工具

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **PM2** | 5.3+ | 进程管理 | Node.js进程管理，支持集群模式 |
| **Jest** | 29.7+ | 单元测试 | Facebook开源的测试框架，功能强大 |
| **Mocha** | 10.2+ | 测试框架 | 功能丰富的JavaScript测试框架 |
| **Chai** | 4.3+ | 断言库 | BDD/TDD风格的断言库 |
| **Supertest** | 6.3+ | API测试 | HTTP请求测试库 |

---

### 1.3 嵌入式技术栈

#### 1.3.1 主控芯片

| 芯片 | 特性 | 用途 | 选型理由 |
|------|------|------|----------|
| **ESP32-WROOM-32** | 双核240MHz，WiFi+蓝牙 | 楼控/客房端 | 性能强大，WiFi+蓝牙，成本低，生态成熟 |
| **ESP32-S2-MINI** | 低功耗，单核 | 前台管理端 | 低功耗，适合USB外设模式 |
| **STM32F103C8T6** | 72MHz，ARM Cortex-M3 | 前台管理端 | 性价比高，适合USB外设模式 |

#### 1.3.2 开发环境

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Arduino IDE** | 2.2+ | 嵌入式开发 | 易上手，社区支持好，适合快速开发 |
| **ESP-IDF** | 5.3+ | 官方开发框架 | 官方开发框架，功能强大，支持所有ESP32特性 |
| **PlatformIO** | 7.4+ | 跨平台开发 | 跨平台开发环境，支持多种开发板 |

#### 1.3.3 通信协议

| 协议 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **MQTT** | 3.1.1 | 设备通信 | 轻量级，适合低带宽网络 |
| **HTTP/HTTPS** | 1.1/2.0 | 数据上报 | 标准协议，易于调试 |
| **WebSocket** | - | 实时通信 | 双向通信，低延迟 |
| **USB串口** | - | 前台管理端通信 | 简单可靠，适合短距离通信 |

#### 1.3.4 传感器

| 传感器 | 类型 | 用途 | 特性 |
|--------|------|------|------|
| **DHT11** | 温湿度传感器 | 环境监测 | 成本低，测量范围：0-50℃，20-90%RH |
| **MQ-135** | 空气质量传感器 | 空气质量监测 | 检测NH3、NOx、CO2等气体 |
| **光敏电阻** | 光照传感器 | 光照强度监测 | 价格便宜，灵敏度高 |
| **HC-SR501** | 红外传感器 | 人体检测 | 检测人体移动，功耗低 |
| **RC522** | RFID模块 | 门禁系统 | 13.56MHz，支持Mifare卡 |

#### 1.3.5 执行器

| 执行器 | 类型 | 用途 | 特性 |
|--------|------|------|------|
| **继电器** | 电子开关 | 设备控制 | 10A/250VAC，支持大功率设备 |
| **RGB LED** | WS2812B | 状态指示 | 可编程RGB LED，支持级联 |
| **电控锁** | 电磁锁 | 门禁控制 | 12V DC，支持常开/常闭 |
| **红外发射管** | 红外模块 | 红外控制 | 支持学习和发射红外信号 |

---

### 1.4 DevOps技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|----------|
| **Docker** | 24.0+ | 容器化部署 | 环境一致性，易于部署和扩展 |
| **Docker Compose** | 2.24+ | 多容器编排 | 简单易用的容器编排工具 |
| **Nginx** | 1.25+ | 反向代理 | 高性能HTTP服务器和反向代理 |
| **Git** | 2.44+ | 版本控制 | 分布式版本控制系统 |
| **GitHub Actions** | - | CI/CD | 自动化CI/CD流程 |
| **PM2** | 5.3+ | 进程管理 | Node.js进程管理，支持集群 |

---

## 📁 项目目录结构

### 2.1 后端项目结构

```
iot-smart-hotel-backend/
├── src/                           # 源代码
│   ├── config/                    # 配置文件
│   │   ├── database.ts           # 数据库配置
│   │   ├── mqtt.ts               # MQTT配置
│   │   ├── ai.ts                 # AI服务配置
│   │   └── index.ts              # 主配置文件
│   │
│   ├── controllers/               # 控制器
│   │   ├── device.controller.ts  # 设备控制器
│   │   ├── user.controller.ts    # 用户控制器
│   │   ├── ai.controller.ts      # AI控制器
│   │   └── index.ts              # 路由索引
│   │
│   ├── services/                  # 业务逻辑层
│   │   ├── device.service.ts     # 设备服务
│   │   ├── user.service.ts       # 用户服务
│   │   ├── ai.service.ts         # AI服务
│   │   └── index.ts              # 服务索引
│   │
│   ├── models/                    # 数据模型
│   │   ├── device.model.ts       # 设备模型
│   │   ├── user.model.ts         # 用户模型
│   │   ├── sensor.model.ts       # 传感器模型
│   │   └── index.ts              # 模型索引
│   │
│   ├── routes/                    # 路由
│   │   ├── v1/                   # API v1
│   │   │   ├── devices.ts        # 设备路由
│   │   │   ├── users.ts          # 用户路由
│   │   │   ├── ai.ts             # AI路由
│   │   │   └── index.ts          # 路由索引
│   │   └── index.ts              # 主路由
│   │
│   ├── middleware/                # 中间件
│   │   ├── auth.ts               # 认证中间件
│   │   ├── logger.ts             # 日志中间件
│   │   ├── error.ts              # 错误处理中间件
│   │   └── index.ts              # 中间件索引
│   │
│   ├── utils/                     # 工具函数
│   │   ├── mqtt.client.ts        # MQTT客户端
│   │   ├── ai.client.ts          # AI客户端
│   │   ├── validator.ts          # 验证器
│   │   ├── logger.ts             # 日志工具
│   │   └── index.ts              # 工具索引
│   │
│   ├── app.ts                     # 应用入口
│   └── server.ts                  # 服务器启动
│
├── sql/                           # SQL脚本
│   ├── schema.sql                 # 数据库结构
│   └── seed.sql                   # 初始化数据
│
├── tests/                         # 测试文件
│   ├── unit/                      # 单元测试
│   └── api/                       # API测试
│
├── docker/                        # Docker配置
│   ├── Dockerfile                 # Docker镜像构建
│   └── docker-compose.yml         # Docker Compose配置
│
├── logs/                          # 日志目录（运行时生成）
├── .env.example                   # 环境变量示例
├── .gitignore                     # Git忽略文件
├── package.json                   # 项目配置
├── tsconfig.json                  # TypeScript配置
└── README.md                      # 项目说明
```

---

### 2.2 Web前端项目结构

```
iot-smart-hotel-web/
├── src/                           # 源代码
│   ├── assets/                    # 静态资源
│   │   ├── styles/               # 样式文件
│   │   │   └── index.scss        # 主样式文件
│   │   └── images/               # 图片资源
│   │
│   ├── components/                # 公共组件
│   │   ├── layout/               # 布局组件
│   │   │   ├── MainLayout.vue    # 主布局
│   │   │   ├── Sidebar.vue       # 侧边栏
│   │   │   └── Header.vue        # 顶部导航
│   │   └── ui/                   # UI组件
│   │       ├── Chart.vue         # 图表组件
│   │       ├── Card.vue          # 卡片组件
│   │       └── Icon.vue          # 图标组件
│   │
│   ├── views/                     # 页面组件
│   │   ├── dashboard/            # 仪表盘
│   │   │   └── Index.vue         # 仪表盘首页
│   │   ├── devices/              # 设备管理
│   │   │   ├── Index.vue         # 设备列表
│   │   │   └── Detail.vue        # 设备详情
│   │   ├── users/                # 用户管理
│   │   │   ├── Index.vue         # 用户列表
│   │   │   └── Profile.vue       # 个人资料
│   │   ├── ai/                   # AI助手
│   │   │   └── Index.vue         # AI助手
│   │   └── login/                # 登录
│   │       └── Index.vue         # 登录页面
│   │
│   ├── router/                    # 路由配置
│   │   ├── index.ts              # 路由实例
│   │   └── routes.ts             # 路由定义
│   │
│   ├── stores/                    # 状态管理
│   │   ├── useAppStore.ts        # 应用状态
│   │   └── useUserStore.ts       # 用户状态
│   │
│   ├── services/                  # API服务
│   │   ├── api.ts                # HTTP客户端
│   │   └── websocket.ts          # WebSocket客户端
│   │
│   ├── utils/                     # 工具函数
│   │   ├── request.ts            # 请求工具
│   │   ├── formatter.ts          # 格式化工具
│   │   └── validator.ts          # 验证工具
│   │
│   ├── types/                     # TypeScript类型定义
│   │   ├── index.ts              # 类型索引
│   │   └── api.d.ts              # API类型定义
│   │
│   ├── App.vue                    # 根组件
│   └── main.ts                    # 应用入口
│
├── public/                        # 公共文件
│   ├── favicon.ico                # 网站图标
│   └── index.html                 # HTML模板
│
├── docker/                        # Docker配置
│   └── nginx.conf                 # Nginx配置
│
├── .env.example                   # 环境变量示例
├── .gitignore                     # Git忽略文件
├── package.json                   # 项目配置
├── tsconfig.json                  # TypeScript配置
├── vite.config.ts                 # Vite配置
└── README.md                      # 项目说明
```

---

### 2.3 移动端项目结构

```
iot-smart-hotel-mobile/
├── src/                           # 源代码
│   ├── assets/                    # 静态资源
│   ├── components/                # 公共组件
│   ├── pages/                     # 页面
│   │   ├── home/                 # 首页
│   │   ├── services/             # 客房服务
│   │   ├── device/               # 设备控制
│   │   └── profile/              # 个人中心
│   ├── router/                    # 路由配置
│   ├── stores/                    # 状态管理
│   ├── services/                  # API服务
│   ├── utils/                     # 工具函数
│   └── App.vue                    # 根组件
│
├── capacitor.config.ts            # Capacitor配置
├── package.json                   # 项目配置
├── tsconfig.json                  # TypeScript配置
└── README.md                      # 项目说明
```

---

### 2.4 嵌入式项目结构

```
iot-smart-hotel-embedded/
├──前台管理端/                      # 前台管理端固件
│   ├── src/
│   │   ├── main.cpp              # 主程序
│   │   ├── config/               # 配置文件
│   │   │   ├── wifi.h            # WiFi配置
│   │   │   └── mqtt.h            # MQTT配置
│   │   ├── drivers/              # 硬件驱动
│   │   │   ├── rfid.cpp          # RFID驱动
│   │   │   ├── rgb_led.cpp       # RGB LED驱动
│   │   │   ├── button.cpp        # 按键驱动
│   │   │   ├──蜂鸣器.cpp         # 蜂鸣器驱动
│   │   │   └── usb_serial.cpp    # USB串口驱动
│   │   ├── services/             # 业务逻辑
│   │   │   ├── mqtt_client.cpp   # MQTT客户端
│   │   │   └── protocol.cpp      # 协议处理
│   │   └── utils/                # 工具函数
│   │       └── json_parser.cpp   # JSON解析
│   ├── platformio.ini            # PlatformIO配置
│   └── .gitignore
│
├──楼控/                           # 楼控固件
│   ├── src/
│   │   ├── main.cpp
│   │   ├── sensors/              # 传感器驱动
│   │   │   ├── dht11.cpp         # 温湿度传感器
│   │   │   ├── mq135.cpp         # 空气质量传感器
│   │   │   └── light_sensor.cpp  # 光照传感器
│   │   ├── actuators/            # 执行器驱动
│   │   │   └── relay.cpp         # 继电器驱动
│   │   ├── mqtt_client.cpp       # MQTT客户端
│   │   └── data_collector.cpp    # 数据采集
│   ├── platformio.ini
│   └── .gitignore
│
├──客房端/                          # 客房端固件
│   ├── src/
│   │   ├── main.cpp
│   │   ├── actuators/            # 执行器驱动
│   │   │   ├── relay.cpp         # 继电器驱动
│   │   │   ├── infrared.cpp      # 红外驱动
│   │   │   └── lock.cpp          # 电控锁驱动
│   │   ├── sensors/              # 传感器驱动
│   │   │   └── rfid.cpp          # RFID驱动
│   │   ├── mqtt_client.cpp       # MQTT客户端
│   │   └── device_controller.cpp # 设备控制器
│   ├── platformio.ini
│   └── .gitignore
│
└── README.md                      # 嵌入式项目说明
```

---

## 🔌 通信协议设计

### 3.1 MQTT协议

#### 3.1.1 主题结构

```
iot/smart/home/
├── cmd/                     # 控制指令
│   ├── device/{device_id}/set    # 设备控制
│   │   { "action": "turn_on", "params": {} }
│   ├── room/{room_id}/set        # 房间控制
│   │   { "action": "all_off", "params": {} }
│   └── broadcast                 # 广播指令
│       { "action": "sync_time", "params": {} }
├── status/                  # 设备状态
│   ├── device/{device_id}        # 设备状态上报
│   │   { "status": "online", "timestamp": 1234567890 }
│   ├── sensor/{device_id}        # 传感器数据
│   │   { "temperature": 25.5, "humidity": 60 }
│   └── room/{room_id}            # 房间状态
│       { "occupancy": true, "temperature": 25.5 }
├── event/                   # 事件通知
│   ├── alarm/{device_id}         # 报警事件
│   │   { "type": "fire", "level": "high" }
│   └── error/{device_id}         # 错误事件
│       { "code": 1001, "message": "Connection lost" }
└── system/                  # 系统消息
    ├── heartbeat                 # 心跳消息
    │   { "timestamp": 1234567890 }
    └── config                    # 配置更新
        { "version": "1.0.0", "url": "http://..." }
```

#### 3.1.2 消息格式

```json
// 控制指令
{
  "action": "turn_on",
  "params": {
    "device_id": "room_101_light",
    "value": true
  }
}

// 设备状态
{
  "device_id": "room_101",
  "status": "online",
  "timestamp": 1234567890,
  "data": {
    "temperature": 25.5,
    "humidity": 60
  }
}

// 传感器数据
{
  "device_id": "room_101_sensor",
  "sensor_type": "temperature",
  "sensor_value": "25.5",
  "timestamp": 1234567890
}

// 报警事件
{
  "device_id": "room_101",
  "event_type": "fire",
  "event_level": "high",
  "timestamp": 1234567890
}
```

---

### 3.2 HTTP API协议

#### 3.2.1 请求格式

```
GET /api/v1/devices HTTP/1.1
Host: localhost:3000
Authorization: Bearer {token}
Content-Type: application/json
```

#### 3.2.2 响应格式

```json
// 成功响应
{
  "code": 200,
  "message": "success",
  "data": {
    "devices": []
  }
}

// 错误响应
{
  "code": 401,
  "message": "Unauthorized",
  "data": null
}
```

---

### 3.3 WebSocket协议

#### 3.3.1 客户端到服务器

```json
// 设备注册
{
  "action": "device:register",
  "data": {
    "device_id": "room_101",
    "device_type": "room_controller"
  }
}

// 设备状态更新
{
  "action": "device:status",
  "data": {
    "device_id": "room_101",
    "status": "online"
  }
}
```

#### 3.3.2 服务器到客户端

```json
// 设备状态更新
{
  "event": "device:status:update",
  "data": {
    "device_id": "room_101",
    "status": "online"
  }
}

// 报警通知
{
  "event": "alarm:notify",
  "data": {
    "device_id": "room_101",
    "alarm_type": "fire",
    "level": "high"
  }
}
```

---

## 📊 数据库设计

### 4.1 数据库表结构

#### 4.1.1 用户表 (users)

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    role VARCHAR(20) DEFAULT 'user',
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 4.1.2 设备表 (devices)

```sql
CREATE TABLE devices (
    id INT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(50) UNIQUE NOT NULL,
    device_type VARCHAR(20) NOT NULL,
    device_name VARCHAR(50) NOT NULL,
    device_key VARCHAR(50) NOT NULL,
    device_status VARCHAR(20) DEFAULT 'offline',
    firmware_version VARCHAR(20),
    last_seen DATETIME,
    room_id INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_device_id (device_id),
    INDEX idx_device_status (device_status),
    FOREIGN KEY (room_id) REFERENCES rooms(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 4.1.3 传感器数据表 (sensor_data)

```sql
CREATE TABLE sensor_data (
    id INT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(50) NOT NULL,
    sensor_type VARCHAR(20) NOT NULL,
    sensor_value VARCHAR(50) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_device_id (device_id),
    INDEX idx_sensor_type (sensor_type),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 4.1.4 控制指令表 (control_commands)

```sql
CREATE TABLE control_commands (
    id INT PRIMARY KEY AUTO_INCREMENT,
    device_id VARCHAR(50) NOT NULL,
    command_type VARCHAR(20) NOT NULL,
    command_value VARCHAR(50) NOT NULL,
    command_status VARCHAR(20) DEFAULT 'pending',
    created_by INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    executed_at DATETIME,
    INDEX idx_device_id (device_id),
    INDEX idx_created_at (created_at),
    FOREIGN KEY (created_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 4.1.5 房间表 (rooms)

```sql
CREATE TABLE rooms (
    id INT PRIMARY KEY AUTO_INCREMENT,
    room_id VARCHAR(20) UNIQUE NOT NULL,
    room_name VARCHAR(50) NOT NULL,
    room_type VARCHAR(20),
    floor VARCHAR(10),
    status VARCHAR(20) DEFAULT 'empty',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_room_id (room_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 🔐 安全设计

### 5.1 认证授权

#### 5.1.1 JWT Token

```json
// Token payload
{
  "id": 1,
  "username": "admin",
  "role": "admin",
  "exp": 1234567890
}
```

#### 5.1.2 设备认证

```
设备启动时发送认证请求：
{
  "device_id": "room_101",
  "device_key": "device_secret_key"
}

后端验证通过后返回Token：
{
  "token": "device_token_123456",
  "expires_in": 86400
}
```

---

### 5.2 数据加密

| 数据类型 | 加密方式 | 说明 |
|----------|----------|------|
| 密码 | bcrypt | 哈希加密 |
| Token | JWT | 签名加密 |
| 传输数据 | TLS/SSL | 网络加密 |
| 敏感数据 | AES | 数据库加密 |

---

## 📦 部署架构

### 6.1 开发环境

```
┌─────────────────────────────────────────────────────────────────┐
│                         开发环境                                  │
│                                                                   │
│  本地开发环境：                                                   │
│  • Node.js + MySQL + Redis + MQTT Broker                       │
│  • VS Code + Docker Desktop                                     │
│  • 热更新开发                                                    │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 6.2 生产环境

```
┌─────────────────────────────────────────────────────────────────┐
│                         生产环境                                  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    云服务器 (Ubuntu)                      │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│  │  │   Nginx      │  │   Node.js    │  │   MySQL      │   │   │
│  │  │  (反向代理)  │  │  (PM2)       │  │  (主从复制)   │   │   │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │   │
│  │         │                  │                  │          │   │
│  │  ┌──────▼───────┘  ┌──────▼───────┘  ┌──────▼───────┘   │   │
│  │  │   Redis      │  │  MQTT Broker │  │   Redis        │   │   │
│  │  │  (缓存集群)   │  │  (消息队列)   │  │  (集群)        │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    CDN 加速                                │   │
│  │  • 前端静态资源                                             │   │
│  │  • 图片、视频等静态资源                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 开发流程

### 7.1 Git分支策略

```
main         # 生产环境代码
├── develop  # 开发分支
│   ├── feature/*  # 功能分支
│   └── hotfix/*   # 紧急修复分支
└── release/*  # 发布分支
```

### 7.2 提交规范

```
feat:     新功能
fix:      修复bug
docs:     文档更新
style:    代码格式（不影响代码运行）
refactor: 重构
perf:     性能优化
test:     测试相关
chore:    构建过程或辅助工具变动
```

### 7.3 版本号规范

```
主版本号.次版本号.修订号
1.0.0

- 主版本号：不兼容的API修改
- 次版本号：向下兼容的功能性新增
- 修订号：向下兼容的问题修正
```

---

## 📊 性能指标

### 8.1 系统性能目标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 响应时间 | < 1秒 | 设备控制指令响应 |
| 并发连接 | 1000+ | 同时在线设备数 |
| QPS | 500+ | 后端API每秒请求数 |
| 数据库查询 | < 100ms | 单次查询响应时间 |
| WebSocket延迟 | < 50ms | 实时数据推送延迟 |
| 系统可用性 | 99.9% | 年度停机时间 < 8.76小时 |

### 8.2 扩展性设计

#### 8.2.1 水平扩展
- **后端服务**：无状态设计，支持多实例部署
- **数据库**：读写分离，主从复制
- **缓存**：Redis集群

#### 8.2.2 垂直扩展
- **数据库**：增加索引，优化查询
- **缓存**：增加缓存容量
- **消息队列**：增加MQTT Broker实例

---

## 📝 开发规范

### 9.1 代码规范

#### 9.1.1 前端规范
- 使用TypeScript
- 遵循Vue 3 Composition API
- 使用ESLint和Prettier
- 组件命名使用PascalCase
- 变量命名使用camelCase
- 常量命名使用UPPER_SNAKE_CASE

#### 9.1.2 后端规范
- 使用TypeScript
- 遵循RESTful API设计规范
- 使用ESLint和Prettier
- 函数命名使用camelCase
- 类命名使用PascalCase
- 常量命名使用UPPER_SNAKE_CASE

#### 9.1.3 嵌入式规范
- 使用Arduino IDE或ESP-IDF
- 遵循C++编码规范
- 使用PlatformIO
- 变量命名使用camelCase
- 常量命名使用UPPER_SNAKE_CASE

---

## 📚 常用命令

### 10.1 后端命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 启动服务
npm run start

# 运行测试
npm test

# 代码检查
npm run lint

# 代码格式化
npm run format
```

### 10.2 前端命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建
npm run preview

# 运行测试
npm test

# 代码检查
npm run lint

# 代码格式化
npm run format
```

### 10.3 Docker命令

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart
```

---

## 📖 参考文档

### 11.1 官方文档

- [Vue.js 3](https://vuejs.org/)
- [Node.js](https://nodejs.org/)
- [MySQL](https://www.mysql.com/)
- [Redis](https://redis.io/)
- [MQTT](https://mqtt.org/)
- [ESP32](https://docs.espressif.com/projects/esp-idf/en/latest/)

### 11.2 第三方文档

- [Ant Design Vue](https://2x.antdv.com/)
- [ECharts](https://echarts.apache.org/)
- [Socket.io](https://socket.io/)
- [Docker](https://docs.docker.com/)

---

**文档版本**：v1.0.0  
**最后更新**：2026年4月4日
