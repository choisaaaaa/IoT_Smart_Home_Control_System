# 智慧酒店物联网控制系统 - 后端服务完整部署指南

## 🚀 快速开始

### 1. 环境准备

确保已安装以下软件：

- Node.js 20.x 或更高版本
- MySQL 8.0 或更高版本
- Redis 7.x 或更高版本
- MQTT Broker (如 Mosquitto)

### 2. 克隆项目

```bash
git clone https://github.com/your-username/iot-smart-hotel.git
cd iot-smart-hotel
```

### 3. 安装依赖

```bash
cd backend/iot-hotel-backend
npm install
```

### 4. 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```env
# 服务器配置
APP_HOST=localhost
APP_PORT=3000
NODE_ENV=development
API_PREFIX=/api/v1

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=iot_hotel_system

# Redis配置
REDIS_URL=redis://localhost:6379

# MQTT配置
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# JWT配置
JWT_SECRET=iot_hotel_system_jwt_secret_2024
JWT_EXPIRES_IN=24h

# 日志配置
LOG_LEVEL=info
LOG_DIR=./logs
```

### 5. 初始化数据库

#### 方式一：使用Docker

```bash
cd ../docker
docker-compose up -d
```

#### 方式二：手动初始化

```bash
# 连接到MySQL
mysql -u root -p

# 执行初始化脚本
source /path/to/schema.sql
```

### 6. 启动服务器

```bash
cd backend/iot-hotel-backend
npm run dev
```

服务器将在 `http://localhost:3000` 启动。

### 7. 测试API

```bash
curl http://localhost:3000/api/v1/health
```

预期响应：

```json
{
  "code": 200,
  "message": "服务正常",
  "timestamp": 1712236800000,
  "version": "2.0.0"
}
```

## 📦 Docker部署

### 1. 克隆项目

```bash
git clone https://github.com/your-username/iot-smart-hotel.git
cd iot-smart-hotel
```

### 2. 启动所有服务

```bash
cd docker
docker-compose up -d
```

### 3. 查看服务状态

```bash
docker-compose ps
```

### 4. 查看日志

```bash
# 所有服务日志
docker-compose logs -f

# 单个服务日志
docker-compose logs -f backend
docker-compose logs -f mysql
docker-compose logs -f redis
docker-compose logs -f mqtt
```

### 5. 停止服务

```bash
docker-compose down
```

## 📊 API接口文档

### 1. 酒店管理接口

#### 获取酒店信息

```
GET /api/v1/hotels
```

响应：

```json
{
  "code": 200,
  "message": "获取酒店信息成功",
  "data": {
    "id": 1,
    "hotel_name": "智联酒店",
    "hotel_address": "北京市朝阳区XX路XX号",
    "hotel_phone": "010-12345678",
    "hotel_star": 4,
    "total_rooms": 100,
    "occupied_rooms": 45,
    "occupancy_rate": 45.00,
    "logo": "http://example.com/logo.png",
    "description": "舒适温馨的酒店",
    "created_at": "2024-01-01 00:00:00",
    "updated_at": "2024-01-01 00:00:00"
  },
  "timestamp": 1712236800000
}
```

#### 更新酒店信息

```
PUT /api/v1/hotels
Authorization: Bearer <token>
Content-Type: application/json

{
  "hotel_name": "智联酒店",
  "hotel_address": "北京市朝阳区XX路XX号",
  "hotel_phone": "010-12345678",
  "hotel_star": 4,
  "logo": "http://example.com/logo.png",
  "description": "舒适温馨的酒店"
}
```

### 2. 房间管理接口

#### 获取房间列表

```
GET /api/v1/rooms?page=1&pageSize=10&status=available&type=standard
Authorization: Bearer <token>
```

响应：

```json
{
  "code": 200,
  "message": "获取房间列表成功",
  "data": {
    "list": [
      {
        "id": 1,
        "room_number": "101",
        "room_type": "standard",
        "room_name": "标准单人房",
        "room_price": 299.00,
        "room_status": "available",
        "floor": 1,
        "area": 25.5,
        "bed_type": "single",
        "max_guests": 1,
        "description": "舒适的标准单人房",
        "facilities": ["WiFi", "电视", "空调", "电话"],
        "images": ["http://example.com/room1.jpg"],
        "created_at": "2024-01-01 00:00:00",
        "updated_at": "2024-01-01 00:00:00"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 10,
    "totalPages": 10
  },
  "timestamp": 1712236800000
}
```

#### 获取房间详情

```
GET /api/v1/rooms/1
Authorization: Bearer <token>
```

#### 创建房间

```
POST /api/v1/rooms
Authorization: Bearer <token>
Content-Type: application/json

{
  "room_number": "103",
  "room_type": "standard",
  "room_name": "标准单人房",
  "room_price": 299.00,
  "room_status": "available",
  "floor": 1,
  "area": 25.5,
  "bed_type": "single",
  "max_guests": 1,
  "description": "舒适的标准单人房",
  "facilities": ["WiFi", "电视", "空调", "电话"],
  "images": ["http://example.com/room3.jpg"]
}
```

#### 更新房间

```
PUT /api/v1/rooms/1
Authorization: Bearer <token>
Content-Type: application/json

{
  "room_number": "101",
  "room_type": "standard",
  "room_name": "标准单人房",
  "room_price": 299.00,
  "room_status": "available",
  "floor": 1,
  "area": 25.5,
  "bed_type": "single",
  "max_guests": 1,
  "description": "舒适的标准单人房",
  "facilities": ["WiFi", "电视", "空调", "电话"],
  "images": ["http://example.com/room1.jpg"]
}
```

#### 删除房间

```
DELETE /api/v1/rooms/1
Authorization: Bearer <token>
```

### 3. 预订管理接口

#### 获取预订列表

```
GET /api/v1/bookings?page=1&pageSize=10&status=pending&guest_name=张三
Authorization: Bearer <token>
```

#### 获取预订详情

```
GET /api/v1/bookings/1
Authorization: Bearer <token>
```

#### 创建预订

```
POST /api/v1/bookings
Authorization: Bearer <token>
Content-Type: application/json

{
  "room_id": 1,
  "guest_name": "张三",
  "guest_phone": "13800138000",
  "guest_id_number": "110101199001011234",
  "check_in_date": "2024-04-05",
  "check_out_date": "2024-04-07",
  "guest_count": 2,
  "special_requests": "需要加床",
  "payment_method": "balance"
}
```

响应：

```json
{
  "code": 200,
  "message": "创建预订成功",
  "data": {
    "id": 1,
    "booking_number": "BK20240405ABCD1234",
    "total_price": 598.00
  },
  "timestamp": 1712236800000
}
```

#### 确认预订

```
PUT /api/v1/bookings/1/confirm
Authorization: Bearer <token>
```

#### 办理入住

```
PUT /api/v1/bookings/1/checkin
Authorization: Bearer <token>
```

#### 办理退房

```
PUT /api/v1/bookings/1/checkout
Authorization: Bearer <token>
```

#### 取消预订

```
PUT /api/v1/bookings/1/cancel
Authorization: Bearer <token>
```

### 4. 支付管理接口

#### 获取支付列表

```
GET /api/v1/payments?page=1&pageSize=10&status=pending&order_type=booking
Authorization: Bearer <token>
```

#### 获取支付详情

```
GET /api/v1/payments/1
Authorization: Bearer <token>
```

#### 创建支付订单

```
POST /api/v1/payments
Authorization: Bearer <token>
Content-Type: application/json

{
  "order_type": "booking",
  "order_id": 1,
  "amount": 299.00,
  "payment_method": "wechat",
  "description": "预订支付"
}
```

#### 支付

```
PUT /api/v1/payments/1/pay
Authorization: Bearer <token>
Content-Type: application/json

{
  "transaction_no": "TXN20240405123456789"
}
```

### 5. 会员管理接口

#### 获取会员列表

```
GET /api/v1/members?page=1&pageSize=10&level=gold
Authorization: Bearer <token>
```

#### 获取会员详情

```
GET /api/v1/members/1
Authorization: Bearer <token>
```

#### 注册会员

```
POST /api/v1/members
Content-Type: application/json

{
  "phone": "13800138001",
  "password": "123456",
  "name": "李四",
  "id_number": "110101199001011235"
}
```

#### 更新会员信息

```
PUT /api/v1/members/1
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "李四",
  "id_number": "110101199001011235",
  "member_level": "gold",
  "points": 1000,
  "balance": 500.00,
  "total_spent": 5000.00,
  "total_stays": 10
}
```

#### 会员登录

```
POST /api/v1/members/login
Content-Type: application/json

{
  "phone": "13800138000",
  "password": "123456"
}
```

响应：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "id": 1,
    "phone": "13800138000",
    "name": "张三",
    "member_level": "standard",
    "points": 100,
    "balance": 50.00
  },
  "timestamp": 1712236800000
}
```

### 6. 优惠券管理接口

#### 获取优惠券列表

```
GET /api/v1/coupons?page=1&pageSize=10&status=unused
Authorization: Bearer <token>
```

#### 获取优惠券详情

```
GET /api/v1/coupons/1
Authorization: Bearer <token>
```

#### 创建优惠券

```
POST /api/v1/coupons
Authorization: Bearer <token>
Content-Type: application/json

{
  "coupon_name": "满200减50优惠券",
  "coupon_type": "discount",
  "discount_value": 50.00,
  "min_amount": 200.00,
  "total_count": 1000,
  "valid_from": "2024-04-05",
  "valid_to": "2024-04-30"
}
```

#### 更新优惠券

```
PUT /api/v1/coupons/1
Authorization: Bearer <token>
Content-Type: application/json

{
  "coupon_name": "满200减50优惠券",
  "coupon_type": "discount",
  "discount_value": 50.00,
  "min_amount": 200.00,
  "total_count": 1000,
  "valid_from": "2024-04-05",
  "valid_to": "2024-04-30"
}
```

#### 删除优惠券

```
DELETE /api/v1/coupons/1
Authorization: Bearer <token>
```

#### 领取优惠券

```
POST /api/v1/coupons/1/receive
Authorization: Bearer <token>
Content-Type: application/json

{
  "member_id": 1
}
```

### 7. 送物订单接口

#### 获取送物订单列表

```
GET /api/v1/delivery?page=1&pageSize=10&status=pending&item_category=food
Authorization: Bearer <token>
```

#### 获取送物订单详情

```
GET /api/v1/delivery/1
Authorization: Bearer <token>
```

#### 创建送物订单

```
POST /api/v1/delivery
Authorization: Bearer <token>
Content-Type: application/json

{
  "room_id": 1,
  "item_category": "food",
  "item_name": "外卖",
  "quantity": 2,
  "note": "不要辣"
}
```

#### 完成送物订单

```
PUT /api/v1/delivery/1/complete
Authorization: Bearer <token>
```

### 8. 报修工单接口

#### 获取报修工单列表

```
GET /api/v1/maintenance?page=1&pageSize=10&status=pending&fault_type=electric&priority=high
Authorization: Bearer <token>
```

#### 获取报修工单详情

```
GET /api/v1/maintenance/1
Authorization: Bearer <token>
```

#### 创建报修工单

```
POST /api/v1/maintenance
Authorization: Bearer <token>
Content-Type: application/json

{
  "room_id": 1,
  "fault_type": "electric",
  "fault_description": "空调不制冷",
  "photos": ["http://example.com/photo1.jpg"],
  "priority": "high"
}
```

#### 分配维修人员

```
PUT /api/v1/maintenance/1/assign
Authorization: Bearer <token>
Content-Type: application/json

{
  "repairer": "王五"
}
```

#### 完成报修工单

```
PUT /api/v1/maintenance/1/complete
Authorization: Bearer <token>
Content-Type: application/json

{
  "repair_description": "更换空调压缩机",
  "repair_cost": 500.00
}
```

### 9. 服务评价接口

#### 获取评价列表

```
GET /api/v1/reviews?page=1&pageSize=10&order_type=booking
Authorization: Bearer <token>
```

#### 获取评价详情

```
GET /api/v1/reviews/1
Authorization: Bearer <token>
```

#### 创建评价

```
POST /api/v1/reviews
Authorization: Bearer <token>
Content-Type: application/json

{
  "order_id": 1,
  "order_type": "booking",
  "member_id": 1,
  "score": 5,
  "content": "服务非常好",
  "photos": ["http://example.com/photo1.jpg"]
}
```

## 🔐 认证授权

### JWT认证

在请求头中添加Authorization字段：

```
Authorization: Bearer <your_jwt_token>
```

### 设备认证

在请求头中添加Device-Id和Device-Key字段：

```
Device-Id: <device_id>
Device-Key: <device_key>
```

## 📝 WebSocket使用

### 连接WebSocket

```javascript
const socket = io('http://localhost:3000');

socket.on('connect', () => {
  console.log('连接成功');
});

socket.on('message', (data) => {
  console.log('收到消息:', data);
});

socket.on('disconnect', () => {
  console.log('连接断开');
});
```

### 加入房间

```javascript
socket.emit('join_room', 'room_101');
```

### 离开房间

```javascript
socket.emit('leave_room', 'room_101');
```

### 发送消息

```javascript
socket.emit('send_message', {
  roomId: 'room_101',
  content: '你好',
  timestamp: Date.now()
});
```

### 接收消息

```javascript
socket.on('receive_message', (data) => {
  console.log('收到消息:', data);
});
```

## 🔧 MQTT使用

### 发布消息

```javascript
import mqttService from './services/mqtt.service';

await mqttService.publish('hotel/device/status', {
  deviceId: 'MAIN001',
  status: 'online',
  timestamp: Date.now()
});
```

### 订阅主题

```javascript
import mqttService from './services/mqtt.service';

await mqttService.subscribe('hotel/device/#');
```

## 📊 数据库配置

### 环境变量

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=iot_hotel_system
```

### 连接池配置

```typescript
const poolOptions = {
  host: config.database.host,
  port: config.database.port,
  user: config.database.user,
  password: config.database.password,
  database: config.database.database,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4_unicode_ci'
};
```

## 🐛 常见问题

### 1. 端口被占用

```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3000
kill -9 <PID>
```

### 2. 数据库连接失败

```bash
# 检查MySQL服务状态
systemctl status mysql

# 检查数据库配置
cat .env

# 测试数据库连接
mysql -u root -p -h localhost -P 3306
```

### 3. 依赖安装失败

```bash
# 清除缓存
npm cache clean --force

# 重新安装
npm install
```

### 4. MQTT连接失败

```bash
# 检查MQTT服务状态
systemctl status mosquitto

# 检查MQTT配置
cat .env

# 测试MQTT连接
mosquitto_sub -h localhost -p 1883 -t "test"
```

## 📚 相关文档

- [项目结构说明](./docs/项目结构说明.md)
- [数据库配置说明](./docs/数据库配置说明.md)
- [启动指南](./docs/启动指南.md)
- [Docker配置说明](./docs/Docker配置说明.md)

---

**版本**: v1.0.0  
**最后更新**: 2026年4月4日
